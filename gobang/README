qt实现的电脑端客户端：
	board类是整个游戏客户端的主体，负责棋盘的绘制，接收鼠标的点击事件
	gamedata类负责存储游戏棋子的位置坐标，下棋的双方ID，下棋的步骤

	board类将主体的棋盘棋子进行绘制，并添加悔棋、开始、时间、头像、昵称的按钮和标签，同时实现悔棋，开始的槽函数，
		在开始游戏后，启动定时器，并且鼠标响应事件有效运行，每一次下棋都会判断输赢。

	为其他的类继承提供虚函数，重新设置标签的位置大小内容，重写开始的槽函数，重点重写鼠标响应事件实现对棋盘的输
		入。

	网络对战，增加聊天的功能。
	人机对战，新增机器智能下子，重写判断输赢的算法。
	完善对开始游戏，结束游戏时的资源的管理。

	增加登录游戏注册账号的类
	增加选择对战模式的类
	根据选择的模式显示游戏界面

	增加在选择模式和游戏界面的功能，增加中途退出游戏，重新开始游戏的功能。
	
	biao qa

